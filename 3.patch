--- a/app/src/main/cpp/jni_bridge_imd.c
+++ b/app/src/main/cpp/jni_bridge_imd.c
@@ -114,7 +114,7 @@ Java_com_superalpha_sideload_bridge_NativeBridge_nativeConnect(
 
     /* Kiểm tra usbmuxd server */
-    const char *sock_path = usbmuxd_server_get_socket_path();
+    const char *sock_path = usbmuxd_server_socket_path();
     if (!sock_path) {
         emit_log("[imd] ❌ usbmuxd server chưa chạy");
         return JNI_FALSE;
@@ -122,7 +122,7 @@ Java_com_superalpha_sideload_bridge_NativeBridge_nativeConnect(
 
     /* ... */
 
-    int r = usb_bridge_init(usbFd);
+    int r = usb_bridge_init_from_fd(usbFd);
     if (r < 0) {
         char msg[128];
         snprintf(msg, sizeof(msg), "[imd] ❌ usb_bridge_init err=%d", r);
@@ -130,7 +130,7 @@ Java_com_superalpha_sideload_bridge_NativeBridge_nativeConnect(
         return JNI_FALSE;
     }
 
-    usb_bridge_cleanup();
+    usb_bridge_close();
