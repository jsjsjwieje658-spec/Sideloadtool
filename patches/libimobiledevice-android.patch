--- a/src/idevice.c
+++ b/src/idevice.c
@@ -245,6 +245,24 @@ LIBIMOBILEDEVICE_API idevice_error_t idevice_new_with_options(idevice_t *device,
 				}
 			}
 		}
+	} else if (udid && (options & IDEVICE_LOOKUP_USBMUX)) {
+		/* Android non-root bypass: create device directly from UDID */
+		idevice_t dev = (idevice_t)malloc(sizeof(struct idevice_private));
+		if (dev) {
+			memset(dev, 0, sizeof(struct idevice_private));
+			dev->udid = strdup(udid);
+			dev->mux_id = 1;
+			dev->version = 0;
+			dev->device_class = 0;
+			dev->conn_type = CONNECTION_USBMUXD;
+			dev->conn_data = NULL;
+			*device = dev;
+			debug_info("idevice_new_with_options: Android bypass for %s", udid);
+			return IDEVICE_E_SUCCESS;
+		} else {
+			return IDEVICE_E_UNKNOWN_ERROR;
+		}
 	}

 	return IDEVICE_E_NO_DEVICE;
